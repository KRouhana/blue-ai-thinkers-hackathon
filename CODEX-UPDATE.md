# Paste into an existing Codex session before its new track prompt

The product requirements and team split changed. Read `docs/parallel-v2/00-shared-contract.md`, then the assigned v2 track file. v2 supersedes conflicting requirements from the earlier three-person plan. Preserve working code and teammates' changes; do not restart or rebuild the repository.

We are now four people. Build only your assigned track.

Fork is a SILENT, PROACTIVE meeting coworker running against a disposable prototype on a local Mac. It listens after a one-time explicit host start and participant notice. Normal discussion such as "What if this button were bigger?" can cause a reversible preview experiment. There is no wake word, no mandatory button click to address the agent, and no TTS in the MVP. Do not implement the old policy that discards every hypothetical suggestion.

Ground intent in finalized conversation turns, visible UI context, recent referents, and relevant code. Do not execute transcript deltas, quotes, negations, unrelated talk, or ambiguous guesses. If uncertain, show a quiet on-screen question or wait for more context. Prototype changes are experiments, not group approval.

Slack is the entry/status/summary interface. The companion on the Mac captures audio and runs the demo. A human shares its browser window in the Slack Huddle. Do not invent a Huddle media API or claim that a standard Slack bot can join/share autonomously.

Support both a blank prepared frontend template and an allowlisted existing repository via a disposable demo copy. Reuse its framework and mock unavailable integrations. Do not generate tests or build real infrastructure for a quick prototype. Only ensure the dev server compiles and the preview renders; show errors and rollback rather than faking success.

Keep one code writer, persistent dev server, reversible snapshots, coalesced changes, and a visible Pause/Undo. Preserve workspace sandboxing and do not expose credentials or production systems. An inferred utterance never authorizes a Git push, issue creation, or production deploy.

Implement your vertical slice against the shared v2 interfaces. Use explicitly labeled mocks for another teammate's unfinished component. Report files changed, actual checks performed, and integration blockers. Do not stop at a plan.

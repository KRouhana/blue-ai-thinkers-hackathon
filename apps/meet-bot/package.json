{
  "name": "meet-bot",
  "private": true,
  "type": "module",
  "description": "Joins a Google Meet call via the Recall.ai meeting-bot API and displays the Fork presenter view (the meeting workspace's live prototype) as the bot's camera output.",
  "scripts": {
    "start": "node --env-file-if-exists=../../.env --import tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fork/perception": "*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.23.1",
    "typescript": "^5.7.0"
  }
}
